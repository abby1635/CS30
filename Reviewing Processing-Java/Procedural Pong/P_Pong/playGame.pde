void playGame () {
  
  ballSquish(); //Procedure to be called during playGame
  //Called when ball bounces (top, bottom, & paddles)
  
  //Draws the ball
  ellipse(width/2, height/2, width/70, width/70);
  
}
